import ProfileLayout from '@/components/dashboard/profileLayout';
import ExclusivesPage from '@/components/pages/user/exclusivesPage/ExclusivesPage';

const Exclusives = () => {
  return (
    <ProfileLayout>
      <ExclusivesPage />
    </ProfileLayout>
  );
};

export default Exclusives;
